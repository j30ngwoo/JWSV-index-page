events {}
http {
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        location = /portfolio {
            return 301 /portfolio/;
        }
    }
}
